angular.module('Pomodoro.services', ['ui.bootstrap', 'ui.keypress', 'rails', 'ng-rails-csrf', 'ngCookies', 'ui.jq'])
@app = angular.module('Pomodoro', ['Pomodoro.services'])

<% if development? %>
@apiRootUrl = "http://localhost:5000"
<% elsif build? %>
@apiRootUrl = "http://pomodoro-tracker.herokuapp.com"
<% else %>
@apiRootUrl = "NO IDEA"
console.log '<%= environment %>'
<% end %>
